import StaffCommissionClient from './StaffCommissionClient';

export const metadata = {
  title: 'จัดการค่าคอม | Admin',
};

export default function StaffCommissionPage() {
  return <StaffCommissionClient />;
}
